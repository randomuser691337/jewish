function pw(set, func) {
    fesw('setup1', 'setupa');
}

function authg() {
    const i = document.getElementById('authp');
    pass = i.value;
    const ok = readf('/system/check');
    if (ok === 'DontModifyOrYouWillBrickWebDesk') {
        dest('oobespace');writepb('setupdone', 'y');
    } else {
        snack('Incorrect password.');
    }
}